#include <stdio.h>

int main(void)
{

    // get number from users
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);
    printf("Number: %d\n", num);



    // get letters from user
    char letter;
    printf("Enter a letter: \n");

    // I ma suspecting that scanf is taking the 'enter character';
    letter = 0;
    scanf("%c", &letter);
    printf("Letter: %d\n", letter);

    printf("Got at the end\n");

    // get multiple different numbers from users
    

    // get strings from user (not recommended).
}