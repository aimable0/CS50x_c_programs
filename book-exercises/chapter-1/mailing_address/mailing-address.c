#include <stdio.h>

int main(void)
{
    // recieves mailing address and prints it

    char first_name[15];
    printf("First_name: ");

    // trying to receive input from a user using a while loop
    // that ends when the user hints enter (detecting: the enter character ,10).

    // char typing = 'y';
    // int i = 0;
    // while (typing == 'y')
    // {
    //     scanf("%c", &first_name);
    //     i++;

    //     if (first_name[i] == 10)
    //     {
    //         printf("Enter Key hit!");
    //         typing = 'n';
    //     }
    // }
    // printf("got it .. collected your name! atleast not sure if successfully hhh");

    // for (int i = 0; i < 15; i++)
    // {
    //     printf("%c", first_name[i]);
    // }
}